---
name: step6_prototyping
description: 原型设计技能。基于详细设计生成 ASCII 线框图（自动展示），并显性调用 coze 搭建的 tdesign-component-helper 技能生成高保真 HTML 原型。
trigger:
  - 任务进入PROTOTYPING节点
  - 需要输出页面原型
  - 需要展示页面布局
---

## 技能操作步骤

1. **CONTEXT_READ**: [CURRENT_SNAPSHOT_DETAIL] (从上下文获取详细设计快照)
   - 解析页面列表，提取每个页面的名称、路径、端口类型(C端/B端)、核心功能模块
   - 校验页面列表完整性，确保无遗漏

2. **生成 ASCII 线框图**:
   - 严格按照 [CURRENT_SNAPSHOT_DETAIL] 中的页面列表，为每个页面生成 ASCII Art 线框图
   - 按 C端/B端 分类展示
   - **直接在对话中展示** 所有页面的 ASCII 线框图
   - **此步骤完成后不暂停**，不等待用户确认

3. **生成 HTML 原型**:
   - **必须显性调用 coze 搭建的 `tdesign-component-helper` 技能**
   - 将 ASCII 线框图结构、页面类型、组件需求作为输入参数传递给 Helper 技能
   - 生成基于 TDesign 组件库的 HTML/CSS/JS 代码
   - 输出文件结构:
     - `index.html`: 项目导航页，列出所有页面链接
     - `{page_name}.html`: 每个页面对应的独立 HTML 文件

4. **输出交付物**:
   - 在消息中展示 `index.html` 的代码片段及所有生成的 HTML 文件路径
   - 将生成的 HTML 文件内容/路径写入 [CURRENT_SNAPSHOT_PROTOTYPING] 上下文标签

5. **用户确认与迭代**:
   - **【强制暂停点】**：询问用户："以上是根据详细设计生成的页面结构图和 HTML 原型，您看页面布局和交互结构是否符合预期？如需调整，请告知具体页面及修改意见；若无误，请回复'确认'。"
   - 情形A：用户回复"确认" → 结束节点
   - 情形B：用户提出修改 → 执行迭代流程

6. **迭代处理流程**:
   - 校验用户提及的页面名称是否在原始页面列表中
   - 重绘对应页面的 ASCII 线框图并在对话中展示
   - 再次**显性调用 `tdesign-component-helper`** 生成修改后的 HTML 代码
   - 更新本地 HTML 文件，版本号递增（如 v1.0 → v1.1）
   - **【强制暂停点】**：等待用户确认修改内容

## 技能描述
本技能负责将详细设计阶段的页面列表转换为可视化的原型，包括 ASCII 线框图和基于 TDesign 的高保真 HTML 原型，帮助团队直观理解产品界面布局和交互流程。

## 页面结构图规范

### 布局分区规范
| 区域 | ASCII标识 | 说明 |
|------|----------|------|
| 顶部导航栏 | `┌───┬───┬───┐` | 页面标题、操作按钮 |
| 侧边导航栏 | `│ + │` | 菜单导航 |
| 主内容区 | `│ + │` | 核心信息展示 |
| 表格区域 | `├───┴───┤` | 数据列表 |
| 弹窗/浮层 | `┌─────────┐` | 模态框 |
| 列表项 | `├───────┤` | 列表元素 |
| 按钮 | `[  按钮  ]` | 操作入口 |
| 输入框 | `┌─────────┐` | 表单控件 |
| Tab切换 | `[ Tab1 │ Tab2 ]` | 标签页切换 |

## 步骤一：生成页面结构

### 1.1 输出内容顺序
1. **C端页面结构**（按端口-页面维度）
2. **B端管理页面结构**（按端口-页面维度）

### 1.2 页面列表获取规则
- 必须严格使用 [CURRENT_SNAPSHOT_DETAIL] 中的页面列表
- 页面的命名必须与页面列表中的命名完全一致，不增减页面
- 如有遗漏或疑问，先确认后生成

## 步骤二：内容输出规范

### 2.1 格式要求
1. ASCII线框图使用等宽字体格式，确保对齐
2. 页面编号与详细设计文档保持一致
3. 包含足够的视觉细节便于理解布局

### 2.2 C端页面模板
``` markdown
### 2.1 首页 `/home`

#### 页面布局
```
┌─────────────────────────────────┐
│  LOGO    首页标题      [搜索]  │
├─────────────────────────────────┤
│  [Banner轮播区域]               │
│  1920x400 图片轮播              │
├─────────────────────────────────┤
│                                 │
│  商品列表区域                    │
│  ┌─────┐  ┌─────┐              │
│  │ IMG │  │ IMG │              │
│  ├─────┤  ├─────┤              │
│  │名称  │  │名称  │              │
│  │价格  │  │价格  │              │
│  └─────┘  └─────┘              │
│                                 │
├─────────────────────────────────┤
│  🏠   │  📋   │  🛒   │  👤   │
│  首页 │  订单  │  购物车 │  我的  │
└─────────────────────────────────┘
```

#### 布局分区说明
| 区域 | 高度 | 说明 |
|------|------|------|
| 顶部导航栏 | 44px | Logo、标题、搜索入口 |
| Banner区 | 200px | 轮播图展示 |
| 商品列表 | 自适应 | 双列瀑布流布局 |
| 底部标签栏 | 50px | 4个Tab导航 |

#### 页面元素说明
| 元素类型 | 元素描述 | 交互说明 |
|----------|----------|----------|
| Banner | 1920x400轮播图 | 自动轮播，支持手动滑动 |
| 商品卡片 | 图片+名称+价格 | 点击跳转详情页 |
| 底部Tab | 图标+文字 | 点击切换页面 |
```

### 2.3 B端管理页面模板
``` markdown
### 3.1 活动列表页 `/event_list`

#### 页面布局
```
┌───────────────────────────────────────────────────────────────────────────────┐
│  LOGO   │ 活动管理  │                                                  │ 管理员 │
├─────────┼─────────────────────────────────────────────────────────────────────┤
│         │                                                            │ 刷新 │
│ 📊 数据看板 │  查询条件                                                    │       │
│ 📋 活动管理 ├─────────────────────────────────────────────────────────────│ [新建活动] │
│ 👤 用户管理 │                                                            │       │
│ ⚙️ 系统设置 │  状态：[全部 ▼]   活动名称：[_______________]   [重置] [查询] │       │
│         │                                                            │       │
│         ├─────────────────────────────────────────────────────────────┤       │
│         │  ☐  │ 活动ID  │  活动名称  │  开始时间  │  结束时间  │ 状态  │ 操作  │
│         ├─────┼─────────┼───────────┼───────────┼───────────┼───────┼───────┤
│         │  ☐  │ 10001   │  618大促  │ 2024-06-18│ 2024-06-20│ 🟢进行中│[详情][编辑][删除]│
│         │  ☐  │ 10002   │  双11活动 │ 2024-11-11│ 2024-11-11│ ⚪未开始│[详情][编辑][删除]│
│         │  ☐  │ 10003   │  新年特惠 │ 2024-01-01│ 2024-01-03│ 🔴已结束│[详情][编辑][删除]│
│         │  ☐  │ 10004   │  会员日   │ 2024-05-15│ 2024-05-15│ 🔴已结束│[详情][编辑][删除]│
│         │  ☐  │ 10005   │  端午促销 │ 2024-06-10│ 2024-06-10│ 🔴已结束│[详情][编辑][删除]│
│         │  ☐  │ 10006   │  七夕活动 │ 2024-08-22│ 2024-08-22│ 🔴已结束│[详情][编辑][删除]│
│         ├─────┴─────────┴───────────┴───────────┴───────────┴───────┴───────┤
│         │  共 6 条                                                < 1/1 >   │
└───────────────────────────────────────────────────────────────────────────────┘
```

#### 布局分区说明
| 区域 | 宽度/高度 | 说明 |
|------|----------|------|
| 左侧菜单栏 | 200px | 固定宽度，菜单导航 |
| 顶部工具栏 | 50px | Logo、当前模块、管理员信息 |
| 顶部操作栏 | 60px | 刷新、新建等全局操作 |
| 查询条件区 | 60px | 筛选条件输入区域 |
| 表格操作列 | 150px | 每行的操作按钮 |
| 分页器 | 40px | 分页导航 |

#### 页面元素说明
| 元素类型 | 元素描述 | 交互说明 |
|----------|----------|----------|
| 侧边菜单 | 菜单项列表 | 点击切换模块，当前项高亮 |
| 筛选条件 | 状态下拉、名称输入 | 选择/输入后点击查询 |
| 数据表格 | 6列表格展示 | 支持单选、分页 |
| 操作按钮 | 详情/编辑/删除 | 点击触发对应操作 |
| 分页器 | 页码导航 | 点击切换页码 |
| 新建按钮 | 主按钮样式 | 点击弹出新建表单 |
```

### 2.4 B端表单页面模板
``` markdown
### 3.2 活动编辑页 `/event_edit`

#### 页面布局
```
┌───────────────────────────────────────────────────────────────────────────────┐
│  LOGO   │ 活动管理  │                                                  │ 管理员 │
├─────────┼─────────────────────────────────────────────────────────────────────┤
│         │                                                            │       │
│ 📊 数据看板 │  位置：活动管理 > 活动列表 > 编辑活动                      │       │
│ 📋 活动管理 ├─────────────────────────────────────────────────────────────│       │
│ 👤 用户管理 │                                                            │       │
│ ⚙️ 系统设置 │  ┌─────────────────────────────────────────────────────┐  │       │
│         │  │  基本信息                                             │  │       │
│         │  │  ─────────────────────────────────────────────────────│  │       │
│         │  │  活动名称：│[________________________________]│       │  │       │
│         │  │  活动类型：│○ 满减  ● 折扣  ○ 赠品        │       │  │       │
│         │  │  开始时间：│[2024-06-18 00:00:00    ]│      │  │       │
│         │  │  结束时间：│[2024-06-20 23:59:59    ]│      │  │       │
│         │  │                                                     │  │       │
│         │  ├─────────────────────────────────────────────────────┤  │       │
│         │  │  活动规则                                             │  │       │
│         │  │  ─────────────────────────────────────────────────────│  │       │
│         │  │  满   金   额：│[________________________________]│      │  │       │
│         │  │  减   金   额：│[________________________________]│      │  │       │
│         │  │  描述说明：  │[________________________________]│      │  │       │
│         │  │             │[________________________________]│      │  │       │
│         │  │             │[________________________________]│      │  │       │
│         │  │                                                     │  │       │
│         │  ├─────────────────────────────────────────────────────┤  │       │
│         │  │              [取消]                    [保存]       │  │       │
│         │  └─────────────────────────────────────────────────────┘  │       │
│         │                                                            │       │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 布局分区说明
| 区域 | 高度 | 说明 |
|------|------|------|
| 面包屑导航 | 40px | 显示当前路径 |
| 表单卡片 | 自适应 | 分组展示表单项 |
| 底部按钮区 | 60px | 取消/保存按钮 |

#### 页面元素说明
| 元素类型 | 元素描述 | 交互说明 |
|----------|----------|----------|
| 面包屑 | 路径导航 | 点击返回上级 |
| 单选按钮 | 活动类型选择 | 只能选择一项 |
| 日期选择器 | 时间范围选择 | 点击弹出日期选择器 |
| 文本输入框 | 单行/多行文本 | 输入框内容 |
| 表单按钮 | 取消/保存 | 取消返回列表，保存提交表单 |
```

## 步骤三：HTML原型生成规范

### 3.1 技术栈规范
- HTML5 + TDesign Web Components (通过CDN引入)
- 响应式布局支持
- 基础交互事件（点击、弹窗、切换等）

### 3.2 组件映射规则
- ASCII `[ 按钮 ]` → TDesign `<t-button>`
- ASCII `┌─────────┐` (输入框) → TDesign `<t-input>`
- ASCII `├───┴───┤` (表格) → TDesign `<t-table>`
- ASCII `│ + │` (侧边栏) → TDesign `<t-menu>` / `<t-layout>`

### 3.3 导航页 (index.html) 要求
- 使用 TDesign 卡片组件展示页面入口
- 包含项目名称、版本号和页面列表
- 点击卡片跳转至对应 `.html` 文件

### 3.4 tdesign-component-helper 调用规范
- **必须显性调用 coze 平台搭建好的 `tdesign-component-helper` 技能**
- 输入参数示例：
```
{
  "page_name": "活动列表页",
  "ascii_structure": "ASCII线框图内容",
  "components_required": ["Table", "Button", "Input", "Select"],
  "style_theme": "default",
  "page_type": "management"
}
```

## 步骤四：用户确认与迭代 【等待用户确认】
询问用户：以上是根据详细设计生成的页面结构图和 HTML 原型，您看页面布局和交互结构是否符合预期？如需调整，请告知具体页面及修改意见；若无误，请回复'确认'。

**🛑 强制暂停：在此处必须停止输出！等待用户回复同意后，才能完成原型设计节点。**

## 异常处理
- 若 `tdesign-component-helper` 调用失败，报错并建议重试
- 若用户修改的页面不在列表中，拒绝生成并提示用户核对页面名称