---
name: 04-analysis
description: 需求分析技能。基于已确认的需求信息，生成初步PRD文档，识别用户类型、业务对象、页面列表和埋点方案。
---

# 04 - Analysis (需求分析)

## 职责
- 构建 PRD 骨架，识别用户角色、页面清单
- 页面编号生成规则
- 事件编号生成规则
- 异常场景编号规则

---

## 加载指引

### 前序节点产出物

从 Memory.json 获取 clarify 的 file 路径：
- 调用 manage.read_memory() 获取 node_list
- 从 node_list 中获取 clarify 的 file 字段
- 读取该文件内容

---

## 技能操作步骤

1. 解析 clarify 产出的文件内容
2. 按照输出模板输出交付物
3. **将生成的 Markdown 内容直接输出到用户消息中**（如果包含 Mermaid 代码块，需要特殊说明以便渲染）
4. 等待用户确认交付物

---

## 技能描述步骤一：需求分析

**🚨 关键规则：初步的 PRD 也必须非常详细，不能只是一个空框架。**

### 1.1 产出内容

根据步骤一的确认信息，直接生成一份详细的初步 **Markdown 格式**文档（同时将文档内容以文本方式输出给用户）。这份初版 PRD 必须搭建好完整的文档结构骨架，并且必须深度填充以下部分：

1. **项目基本信息**
2. **需求背景与目标**：先说明需求背景，然后同列表的方式展示业务目标。业务目标必须包含四列表格（目标类型、描述、衡量指标、目标值）。
3. **识别用户类型和端口**：
    - 识别用户类型和端口。
    - 根据用户类型和端口，梳理用户在使用产品时的场景和核心流程。（先使用文字描述流程节点，如用户下单-》订单确认-》订单支付）
4. **识别业务对象**：用 Markdown 表格输出业务对象和关键属性（如用户、订单、商品等），表格格式如下：

```markdown
| 业务对象 | 对象属性 |
|---------|---------|
| 用户 | 用户ID、用户名、手机号、邮箱、注册时间、最后登录时间、用户等级、积分余额 |
| 订单 | 订单ID、用户ID、订单状态、订单金额、支付方式、下单时间、支付时间 |
| 商品 | 商品ID、商品名称、商品描述、商品价格、库存数量、兑换所需积分、上架状态 |
```

**🚨 必须使用上述表格格式！**

5. **业务流程图**：
  - 用 Mermaid 图表输出业务流程图，展示用户在使用产品时的场景和核心流程。
6. **页面流程和页面列表**：
  - 用文字描述的方式输出B、C端页面流程，如：首页-》商品详情-》订单确认-》订单支付-》订单详情
  - 用列表输出B、C端页面列表，需要按照页面个能和用户操作两个维度描述。
7. **埋点方案**：必须使用表格输出埋点场景的埋点的字段。
8. **异常场景**：对接系统返回的异常状态处理。（接口调用异常、接口返回业务异常）
*（后续章节如详细方案带原型的模块、整体流程图等可先留空，并注明待原型确认后补充）*

### 1.2 用户确认 【等待用户确认】

询问用户：这是产品的详细第一版架构和业务逻辑，您看方向和基础逻辑准确吗？如果没问题，我们将先进行原型设计，通过具体的画面来进一步理清交互细节和可能遗漏的功能。

**🛑 强制暂停：在此处必须停止输出！等待用户回复同意后，才能进入步骤四。**

---

## 输出文件

- 文件名: `analysis.md`
- 路径: `./output/V{version}/analysis/analysis.md`
- 生成规则: 见 [01-router.md](01-router.md) 中的文件生成规则

---

## 完成后

1. 保存产出到文件：`./output/V{version}/analysis/analysis.md`
2. 调用 manage.update_node_output("analysis", "output/V{version}/analysis/analysis.md")
3. 展示产出摘要
4. 等待用户确认
5. 确认后 → 调用 manage.update_node_status("analysis", "CONFIRM")
6. 确认后 → 调用 manage.transition_to_next_node()
7. 确认后 → 调用 manage.persist_memory()
8. 进入下一节点
